## Quick Start

```javascript
npm install
```

### Development

To start the project locally, run:

```javascript
npm run dev
```

### Start server

To start the project locally, run:

```javascript
npm run build
npm run start
```

### Generate static file

To start the project locally, run:

```javascript
npm run static
```
